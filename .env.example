# Copy to .env and fill in your values (do not commit .env)

# Azure OpenAI - from Azure Portal: your resource → Keys and endpoint
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=...
# Deployment name (the name you gave the model in Azure OpenAI Studio, e.g. gpt-4o)
AZURE_OPENAI_DEPLOYMENT=gpt-4o
# Optional; default 2024-02-15-preview
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# MongoDB Atlas - from Atlas UI: Connect → Drivers → connection string
# Replace <password> with your DB user password
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/

# Voyage AI - get at https://dash.voyageai.com/ (embeddings + reranker)
VOYAGE_API_KEY=pa-...
# Optional: embedding model (default voyage-3.5-lite, 512 dims). Use any from your account, e.g. voyage-4-lite
# VOYAGE_EMBED_MODEL=voyage-3.5-lite

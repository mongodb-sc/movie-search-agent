# Requires Python >=3.10 (needed for mcp). Create venv with: python3.10 -m venv .venv (or python3.11 / python3.12)
openai>=1.0.0
mcp>=1.0.0
voyageai>=0.2.0
pymongo>=4.6.0
python-dotenv>=1.0.0

# LangGraph orchestrator + MongoDB checkpointer (short-term) and long-term memory
langgraph>=0.2.0
langgraph-checkpoint-mongodb>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
